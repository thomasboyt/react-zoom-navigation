import React from "react";

const DetailShort = () => (
  <div className="page-container card-page-container">
    <div style={{ padding: "10px" }}>
      <h2>This is a detail page!</h2>

      <p>
        This page does not have much content, so it doesn't get scrolled or
        anything.
      </p>
    </div>
  </div>
);

export default DetailShort;
